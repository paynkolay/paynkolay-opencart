{{ header }}{{ column_left }}
<div id="content">
	<div class="page-header">
		<div class="container-fluid">
			<div class="float-end">
        <button type="submit" form="form-payment" data-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fa fa-save"></i></button>
        <a href="{{ back }}" data-bs-toggle="tooltip" title="{{ button_back }}" class="btn btn-light"><i class="fas fa-reply"></i></a>
      </div>
      <h1>{{ heading_title }}</h1>
      <ul class="breadcrumb">
        {% for breadcrumb in breadcrumbs %}
        <li class="breadcrumb-item"><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
        {% endfor %}
      </ul>
		</div>
	</div>
	<div class="container-fluid">
		
    {% if error_warning %}
    <div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> {{ error_warning }}
      <button type="button" class="close" data-dismiss="alert">&times;</button>
    </div>
    {% endif %}
    <div class="card">
    <div class="card-header"><i class="fa fa-pencil"></i> {{ text_edit }}
      </div>
      <div class="card-body">
        <form ction="{{ save }}" method="post" enctype="multipart/form-data" id="form-payment">
          <div class="row mb-3 form-group required">
            <label class="col-sm-2 col-form-label" for="input-sx">{{ entry_sx }}</label>
            <div class="col-sm-10">
              <input type="text" name="payment_nkolaypos_sx" value="{{ payment_nkolaypos_sx }}" placeholder="{{ entry_sx }}" id="input-sx" class="form-control" />
              {% if error_sx %}
              <div class="text-danger">{{ error_sx }}</div>
              {% endif %}
            </div>
          </div>
          <div class="row mb-3 form-group required">
            <label class="col-sm-2 col-form-label" for="input-secret">{{ entry_secret }}</label>
            <div class="col-sm-10">
              <input type="text" name="payment_nkolaypos_secret" value="{{ payment_nkolaypos_secret }}" placeholder="{{ entry_secret }}" id="input-secret" class="form-control" />
              {% if error_secret %}
              <div class="text-danger">{{ error_secret }}</div>
              {% endif %}
            </div>
          </div>

          <div class="row mb-3 form-group">
            <label class="col-sm-2 col-form-label" for="input-mode">Form Türü</label>
            <div class="col-sm-10">
              <select name="payment_nkolaypos_form_turu" id="input-mode" class="form-control">
                <option value="2" selected="selected">Pay N Kolay Ödeme</option>
              </select>
            </div>
          </div>

          <div class="row mb-3 form-group">
            <label class="col-sm-2 col-form-label" for="input-mode">Test Mode</label>
            <div class="col-sm-10">
              <select name="payment_nkolaypos_mode" id="input-mode" class="form-control">
                {% if payment_nkolaypos_mode == '0' %}
                <option value="0" selected="selected">Pasif</option>
                {% else %}
                <option value="0">Pasif</option>
                {% endif %}
                {% if payment_nkolaypos_mode == '1' %}
                <option value="1" selected="selected">Aktif</option>
                {% else %}
                <option value="1">Aktif</option>
                {% endif %}>
              </select>
            </div>
          </div>
          <div class="row mb-3 form-group">
            <label class="col-sm-2 col-form-label" for="input-type">{{ entry_type }}</label>
            <div class="col-sm-10">
              <select name="payment_nkolaypos_type" id="input-type" class="form-control">
                {% if payment_nkolaypos_type == '3D' %}
                <option value="3D" selected="selected">{{ text_pay }}</option>
                {% else %}
                <option value="3D">{{ text_pay }}</option>
                {% endif %}
                {% if payment_nkolaypos_type == 'Normal' %}
                <option value="Normal" selected="selected">{{ text_card }}</option>
                {% else %}
                <option value="Normal">{{ text_card }}</option>
                {% endif %}>
              </select>
            </div>
          </div>
          <div class="row mb-3 form-group">
            <label class="col-sm-2 col-form-label" for="input-total"><span data-toggle="tooltip" title="{{ help_total }}">{{ entry_total }}</span></label>
            <div class="col-sm-10">
              <input type="text" name="payment_nkolaypos_total" value="{{ payment_nkolaypos_total }}" placeholder="{{ entry_total }}" id="input-total" class="form-control" />
            </div>
          </div>
          <div class="row mb-3 form-group">
            <label class="col-sm-2 col-form-label" for="input-order-status">{{ entry_order_status }}</label>
            <div class="col-sm-10">
              <select name="payment_nkolaypos_order_status_id" id="input-order-status" class="form-control">
                {% for order_status in order_statuses %}
                {% if order_status.order_status_id == payment_nkolaypos_order_status_id %}
                <option value="{{ order_status.order_status_id }}" selected="selected">{{ order_status.name }}</option>
                {% else %}
                <option value="{{ order_status.order_status_id }}">{{ order_status.name }}</option>
                {% endif %}
                {% endfor %}
              </select>
            </div>
          </div>
          <div class="row mb-3 form-group">
            <label class="col-sm-2 col-form-label" for="input-geo-zone">{{ entry_geo_zone }}</label>
            <div class="col-sm-10">
              <select name="payment_nkolaypos_geo_zone_id" id="input-geo-zone" class="form-control">
                <option value="0">{{ text_all_zones }}</option>
                {% for geo_zone in geo_zones %}
                {% if geo_zone.geo_zone_id == payment_nkolaypos_geo_zone_id %}
                <option value="{{ geo_zone.geo_zone_id }}" selected="selected">{{ geo_zone.name }}</option>
                {% else %}
                <option value="{{ geo_zone.geo_zone_id }}">{{ geo_zone.name }}</option>
                {% endif %}
                {% endfor %}
              </select>
            </div>
          </div>
          <div class="row mb-3 form-group">
            <label class="col-sm-2 col-form-label" for="input-status">{{ entry_status }}</label>
            <div class="col-sm-10">
              <div class="form-check form-switch form-switch-lg">
                <input type="hidden" name="payment_nkolay_status" value="0"/>
                <input type="checkbox" name="payment_nkolay_status" value="1" id="input-status" class="form-check-input"{% if payment_nkolay_status %} checked{% endif %}/>
              </div>
            </div>
          </div>
          <div class="row mb-3 form-group">
            <label class="col-sm-2 col-form-label" for="input-sort-order">{{ entry_sort_order }}</label>
            <div class="col-sm-10">
              <input type="text" name="payment_nkolay_sort_order" value="{{ payment_nkolay_sort_order }}" placeholder="{{ entry_sort_order }}" id="input-sort-order" class="form-control" />
            </div>
          </div>
        </form>
      </div>
		</div>
	</div>
</div>	
{{ footer }}